export default function Intro() {
  return (
    <p>
      So there are two kinds of routing, and I think the newer one is is the app
      style (maybe new in v13?), which this app uses, but this{" "}
      <em>particular</em> page is using the older style, the pages directory
      style. Both can be used in the same app, for the purposes of being able to
      migrate a little at a time, as demonstrated here.
    </p>
  )
}
